- name: debug ansible version
  shell: "ansible --version"
  delegated_to: localhost
  register: ansible_debug
- name: debug
  debug:
    var: ansible_debug
- name: debug
  debug:
    var: vlans
- name: add vlan test
  ios_config:
    backup: yes
    src: vlan.j2
    match: none