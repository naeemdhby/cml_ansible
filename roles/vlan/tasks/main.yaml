- name: Show VLAN
  ios_command:
    host: "{{ ansible_host }}"
    username: "{{ ciscousername }}"
    password: "{{ ciscopassword }}"
    commands: show version
  register: show_vlan

- debug: var=show_vlan.stdout_lines
