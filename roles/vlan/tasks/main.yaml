# - name: debug to jason
#   debug:
#     msg: "{{ vlans['101'] }}"
#   register: jason_vlan
#   ignore_errors: yes

- name: add vlan test
  ios_config:
    backup: yes
    src: vlan.j2
    match: none